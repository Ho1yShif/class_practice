package Episode8;

import java.util.Scanner;

public class multiFormat {
	// Logical AND 
	if (condition1 && condition2) {
		<instructions to execute if both conditions evaluate to true>
	  }

	// Logical OR 
	if (condition1 || condition2) {
		<instructions to execute if one or more conditions evaluate to true>
	  }
}